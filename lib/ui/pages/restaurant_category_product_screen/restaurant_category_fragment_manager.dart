


import 'package:flutter/material.dart';
import 'package:practice/bloc/catalog_bloc/catalog_bloc.dart';
import 'package:practice/models/restaurant/restaurant.dart';
/*
Widget fragmentManager({
  required ChangedChainState catalogState,
  required
}) {}) {
  return null;
}

if (!state.restaurantChain.unique || state.restaurant != null) SliverGrid(
            gridDelegate: SliverGridDelegateWithFixedCrossAxisCount(
              crossAxisCount: 2,
            ),
            delegate: SliverChildBuilderDelegate(
                  (context, index) => InkWell(
                onTap: () async => await AutoRouter.of(context).push(RestaurantProductListRoute()),
                child: shortCard(imageUrl: '', title: 'Product $index'),
              ),
              childCount: 100,
            ),
          ),


*/