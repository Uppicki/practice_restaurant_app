



import 'package:flutter/material.dart';

class ProgressIndicatorFragment extends StatelessWidget {
  const ProgressIndicatorFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
